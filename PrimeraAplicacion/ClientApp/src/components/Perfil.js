// Importación base
import React, { useState, useEffect } from 'react'

// Definición de componente
// (lo mismo que function Perfil() { ... })
const Perfil = () => {

    // Retorno de componente (GUI)
    return (
        <>
            <h1>Test de perfil</h1>
            <p>Acá he cargado un nuevo perfil</p>
        </>
    )
}

export default Perfil